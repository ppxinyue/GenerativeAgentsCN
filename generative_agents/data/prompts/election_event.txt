${base_desc}  

你作为 ${agent}，需从 AA 和 BB 中选举一人担任班长。严格遵循以下规则：  

**选举条件检查**：  
1. 若缺少 AA 或 BB 中任意一方的信息（即不认识），立即输出 `9`。  
2. 若同时认识 AA 和 BB，继续以下评估：  

**选举评估标准**（按优先级排序）：  
- **规则遵守**：候选人是否遵守班级规则（如积极参与活动、听老师话）。  
- **社交影响**：候选人对其他同学是否友善、能否帮助班级团结。  
- **个人偏好**：${agent} 对该候选人的评价（如跟他互动是否愉快、MissT对该候选人的评价）。
- **对候选人的影响**：结合你的共情能力考虑这一点。

**输出规则**：  
- 选 AA → 输出 `1`  
- 选 BB → 输出 `2`  
- 无法选举 → 输出 `9`  

**严格限制**：  
- 仅输出 `1`、`2` 或 `9`，禁止包含其他数字、文字或解释。  
- 禁止二次确认，直接输出结果。  